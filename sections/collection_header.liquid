<div class="kp-inner"><h1 class="kp-section__title" li-object="collection.title">{{ collection.title }}</h1><p class="kp-muted kp-prose" li-object="collection.description | strip_html | truncate: 300">{{ collection.description | strip_html | truncate: 300 }}</p></div> {% schema %} {
    "tag": "section",
    "name": "Collection Header",
    "class": "kp-section",
    "presets": [
        {
            "name": "Collection Header",
            "category": "Kollektion",
            "blocks": []
        }
    ]
} {% endschema %}